WITH base_list AS (select * 
from customers
WHERE 	first_name = 'Jill' OR first_name = 'Eva')

SELECT b.first_name, o.order_date,o.order_details,o.total_order_cost
FROM orders o
INNER JOIN base_list b
ON b.id= o.cust_id

ORDER BY b.id
